<script>
export default {
  async asyncData({ $content }) {
    // Cargar todos los archivos de estudios en la carpeta 'studios'
    const studios = await $content('studios').fetch();
    console.log(studios);  // Verifica que los datos estén correctos en la consola
    return { studios };  // Devuelve los estudios como un objeto
  }
}
</script>

<template>
  <div class="container">
    <HeaderView />
    <h3 style="margin-top: 25px; text-align: center;">Estudios Registrados</h3>
    <div style="display: grid; grid-template-columns: repeat(4, 1fr); gap: 30px;">
      <!-- Recorre los estudios obtenidos -->
      <div v-for="studio in studios" :key="studio.slug">
        <NuxtLink :to="{ name: 'studios-slug', params: { slug: studio.slug } }" style="text-decoration: none;">
          <div style="width: 200px; height: 200px; display: flex; align-items: center; justify-content: center; border: 1px solid #000000; border-radius: 10px;">
            <p style="font-size: 24px; font-weight: 700; text-align: center; color: darkmagenta;">{{ studio.name }}</p>
          </div>
        </NuxtLink>
      </div>
    </div>
    <FooterView />
  </div>
</template>
